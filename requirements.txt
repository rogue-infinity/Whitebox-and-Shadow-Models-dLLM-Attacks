# Python 3.10+ required
#
# torch/torchvision are NOT pinned here — they must be installed separately
# matching your CUDA version. See README.md for the recommended install command.
# (Pinning torch via extra-index-url causes uv to resolve requests==2.28.1,
# which conflicts with datasets' requirement of requests>=2.32.2.)

transformers>=4.46,<5.0
datasets>=2.20,<3.0
peft>=0.10              # required by SAMA/attack/misc/models.py (module-level import)
accelerate>=0.30
sentencepiece
huggingface_hub

# Experiment tracking (optional but strongly recommended)
wandb

# Classifiers
xgboost
lightgbm>=4.3
scikit-learn>=1.3

# Feature analysis & visualisation
shap>=0.45
scipy>=1.11
umap-learn>=0.5
matplotlib>=3.7
seaborn>=0.13

# Utilities
numpy
tqdm
tabulate
